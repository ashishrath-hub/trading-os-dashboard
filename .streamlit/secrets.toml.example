# Streamlit Cloud secrets (add in dashboard settings, NOT in git)
# For local testing, copy this to secrets.toml

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-anon-public-key"
